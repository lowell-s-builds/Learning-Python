# Day 7 of learning Python - Sets

# according to what sets are they are like lists (can be changed) but the difference is there can't be duplicates
# there may be more but this is what my initial thoughts are

# sets use {} curly brackets along with set()

# syntax
st = {'item1', 'item2', 'item3', 'item4'}
print(st)
st.update(['item5','item6','item7'])
print(st)

# why do they mix up? is this the unordered case? 

# sets
it_companies = {'Facebook', 'Google', 'Microsoft', 'Apple', 'IBM', 'Oracle', 'Amazon'}
A = {19, 22, 24, 20, 25, 26}
B = {19, 22, 20, 25, 26, 24, 28, 27}
age = [22, 19, 24, 25, 26, 24, 25, 24]

# Find the length of the set it_companies

print('Length of the set:',len(it_companies))

# Add 'Twitter' to it_companies

it_companies.add('Twitter')
print(it_companies)

# Insert multiple IT companies at once to the set it_companies

it_companies.update(['Meta','SpaceX','NASA'])
print(it_companies)

# Remove one of the companies from the set it_companies

it_companies.remove('Facebook')
print(it_companies)

# What is the difference between remove and discard

# discard and remove removes a selected item however if remove() doesn't find the item it gives and error. Discard doesn't produce that error

# Join A and B

C = A.union(B)
print(C)

# Find A intersection B

print(A.intersection(B))

# Is A subset of B

print(A.issubset(B))

print(B.issubset(A))

# Are A and B disjoint sets

print(A.isdisjoint(B))

# Join A with B and B with A

print(B.union(A)) # this might be wrong?

print(A.union(B))

# What is the symmetric difference between A and B

print(A.symmetric_difference(B))

# Delete the sets completely

del A 

# Convert the ages to a set and compare the length of the list and the set, which one is bigger?

new_ages = set(age) 
print('Length of age',len(age))
print('Length of new age',len(new_ages))

# Explain the difference between the following data types: string, list, tuple and set

# string is a 
# list is a collection or un ordered 
# ngl what exact differences there are. I will study them

# I am a teacher and I love to inspire and teach people. How many unique words have been used in the sentence? 
# Use the split methods and set to get the unique words.

unique = "I am a teacher and I love to inspire and teach people."

unique_words = unique.split()

unique_words = set(unique_words)

print('There is {} unique words in the sentance'.format(len(unique_words)))
